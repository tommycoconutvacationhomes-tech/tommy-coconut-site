# Tommy Coconut Redirects
# Preserve SEO value from old Cartel URL to new Clan URL

RewriteEngine On

# 301 Permanent Redirect from cartel.html to clan.html
RewriteRule ^cartel\.html$ /clan.html [R=301,L]

# Additional security headers
<IfModule mod_headers.c>
    Header set X-Content-Type-Options "nosniff"
    Header set X-Frame-Options "SAMEORIGIN"
    Header set X-XSS-Protection "1; mode=block"
</IfModule>